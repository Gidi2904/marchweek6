// string method 
const sentence =`the quick brown fox jumps over the lazy dog`

// length of a string 
console.log(sentence.length)

// convert to uppercase
document.write(sentence.toUpperCase())

// covert to lowercase
document.write(sentence.toLowerCase())

// cut some part of a string 
console.log(sentence.slice(0,10))

// finding index
console.log(sentence[sentence.length -1])

// finding index of fox form position 20
console.log(sentence.indexOf('fox',20))

// finding the index of foxes
console.log(sentence.indexof ('foxes'))

// replace a particular thing 
console.log(sentence.replace('dog','goat'))

// replace all 8 letter with o
console.log(sentence.replaceAll('o','o'))

// check if jump exit
console.log(sentence.include('jump'))

// check if sentence start with 'the'
console.log(sentence.startWith ('the'))

// check if sentence start with 'The'
console.log(sentece.startwith('The'))

// ad strings together
console.log('Alphabets:'.cocat(sentence))

// Find where the strings ends
console.log(sentece.endWith('dog'))

// Find a character at a particular index 

console.log(sentence.charAt(16))

// convert  a string to n array 
console.log(sentence.split(''))

// repeat a particular strings
console.log ('sentence'.repeat(2))

const num =80
console.log(typeof num. toString())

console.log(string(num))






